public interface Imprimivel {
  /* interface = acontece quando uma classe define somente uma unica operação, e nao conseguimos definir uma implementação, somente suas subclasses irão implementar esse método. Numa interface todos os méodos são abstratos, implicitamente. Numa interface definimos apenas a assinatura do método abstrato.. */
  public void imprimir();
   
  
}